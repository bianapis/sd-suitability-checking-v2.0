apiVersion: v1
kind: Service
metadata:
  name: {{ template "service-domain-suitability-checking.fullname" . }}
  labels:
    app: {{ template "service-domain-suitability-checking.name" . }}
    chart: {{ template "service-domain-suitability-checking.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "service-domain-suitability-checking.name" . }}
    release: {{ .Release.Name }}
